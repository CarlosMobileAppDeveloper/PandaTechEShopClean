using System;
namespace PandaTechEShop.Constants
{
    public static class SecureStorageConstants
    {
        // HACK - For testing purposes only. Used for refreshing token but this isn't implemented properly yet
        public const string EmailKey = nameof(EmailKey);
        public const string PasswordKey = nameof(PasswordKey);
    }
}
